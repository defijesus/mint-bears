{
  "name": "mint-bears",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ mint-bears",
    "create-local": "graph create --node http://localhost:8020/ mint-bears",
    "remove-local": "graph remove --node http://localhost:8020/ mint-bears",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mint-bears"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
